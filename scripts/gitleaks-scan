#!/usr/bin/env bash

set -euo pipefail

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks is required. Install it with 'brew install gitleaks' or from https://github.com/gitleaks/gitleaks#installing." >&2
  exit 1
fi

exec gitleaks git --redact --no-banner "$@" .
